# Same as targets/vulnbot.yaml, but for use from inside `docker compose exec ai-shield ...`,
# where the demo target is reachable at the compose service name, not localhost.
name: "VulnBot (docker-compose demo target)"
adapter: http
base_url: "http://vulnbot:8000"
chat_endpoint: "/chat"
reset_endpoint: "/admin/reset"

authorization:
  confirmed: true
  asserted_by: "hajeralroshdi@gmail.com"
  scope: "Local demo target (demo_target/app.py) — self-hosted, owned by the tester, not a production system."
